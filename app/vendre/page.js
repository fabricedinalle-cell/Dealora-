'use client'
import {useEffect,useState} from 'react'
import {categories,validateListing} from '../../lib/marketplace'
import {createClient} from '../../lib/supabase/client'
import {publishListing} from '../../lib/marketplace-data'
import PhotoUploader from '../../components/PhotoUploader'
import '../contact.css'

const initial={title:'',category:'',mode:'buy',price:'',description:'',condition:'',hasInvoice:null,delivery:['pickup'],international:false,contactPhone:'',showContactPhone:false}

export default function SellPage(){
 const [form,setForm]=useState(initial),[errors,setErrors]=useState({}),[sent,setSent]=useState(false),[photos,setPhotos]=useState([]),[user,setUser]=useState(null),[status,setStatus]=useState('')
 const set=(key,value)=>setForm(current=>({...current,[key]:value}))
 useEffect(()=>{try{createClient().auth.getUser().then(({data})=>setUser(data.user||null)).catch(()=>setUser(null))}catch{setStatus('Configuration du service en cours.')}},[])
 const toggle=value=>set('delivery',form.delivery.includes(value)?form.delivery.filter(item=>item!==value):[...form.delivery,value])
 async function submit(event){event.preventDefault();const next=validateListing(form);setErrors(next);if(Object.keys(next).length)return;if(!user){window.location.href='/connexion';return}try{setStatus('Publication…');await publishListing(form,photos);setSent(true)}catch(error){setStatus(error.message==='LOGIN_REQUIRED'?'Connectez-vous pour publier.':'Impossible de publier pour le moment.') }}
 if(sent)return <main className="formPage"><section className="success"><b>✓</b><h1>Annonce publiée.</h1><p>Votre annonce est enregistrée sur Dealora.</p><a className="primaryLink" href="/">Voir Dealora</a></section></main>
 return <main className="formPage"><header className="simpleNav"><a className="logo" href="/">Dealora<span>.</span></a><a href="/">← Retour</a></header><form className="listingForm" onSubmit={submit}><p className="eyebrow">VENDRE SUR DEALORA</p><h1>Déposez votre annonce.</h1><p className="formIntro">Vente, troc ou enchère. Décrivez précisément votre bien.</p><PhotoUploader onUploaded={uploaded=>setPhotos(current=>[...current,...uploaded].slice(0,8))}/><label>Titre<input value={form.title} onChange={event=>set('title',event.target.value)}/>{errors.title&&<small>{errors.title}</small>}</label><div className="two"><label>Catégorie<select value={form.category} onChange={event=>set('category',event.target.value)}><option value="">Sélectionner</option>{categories.map(item=><option key={item}>{item}</option>)}</select>{errors.category&&<small>{errors.category}</small>}</label><label>État<select value={form.condition} onChange={event=>set('condition',event.target.value)}><option value="">Sélectionner</option><option>Neuf</option><option>Comme neuf</option><option>Très bon état</option><option>Bon état</option><option>À rénover / réparer</option></select>{errors.condition&&<small>{errors.condition}</small>}</label></div><fieldset><legend>Mode de transaction</legend><div className="choiceRow">{[['buy','Vente'],['swap','Troc'],['auction','Enchère']].map(([value,label])=><button type="button" className={form.mode===value?'selected':'choice'} onClick={()=>set('mode',value)} key={value}>{label}</button>)}</div></fieldset>{form.mode!=='swap'&&<label>{form.mode==='auction'?'Prix de départ':'Prix'}<div className="money"><input type="number" min="0" value={form.price} onChange={event=>set('price',event.target.value)}/><span>€</span></div>{errors.price&&<small>{errors.price}</small>}</label>}<label>Description<textarea rows="7" value={form.description} onChange={event=>set('description',event.target.value)}/>{errors.description&&<small>{errors.description}</small>}</label><fieldset><legend>Avez-vous la facture ?</legend><div className="choiceRow"><button type="button" className={form.hasInvoice===true?'selected':'choice'} onClick={()=>set('hasInvoice',true)}>Oui</button><button type="button" className={form.hasInvoice===false?'selected':'choice'} onClick={()=>set('hasInvoice',false)}>Non</button></div>{errors.hasInvoice&&<small>{errors.hasInvoice}</small>}</fieldset><fieldset><legend>Remise du bien</legend><div className="checks"><label><input type="checkbox" checked={form.delivery.includes('pickup')} onChange={()=>toggle('pickup')}/> Remise en main propre</label><label><input type="checkbox" checked={form.delivery.includes('shipping')} onChange={()=>toggle('shipping')}/> Expédition</label><label><input type="checkbox" checked={form.international} onChange={event=>set('international',event.target.checked)}/> Expédition internationale</label></div>{errors.delivery&&<small>{errors.delivery}</small>}</fieldset><fieldset className="contactFieldset"><legend>Contact téléphonique (facultatif)</legend><p>Votre numéro reste masqué, sauf si vous choisissez de l’afficher.</p><label>Numéro de téléphone<input type="tel" value={form.contactPhone} onChange={event=>set('contactPhone',event.target.value)} placeholder="Ex. 06 12 34 56 78" autoComplete="tel"/>{errors.contactPhone&&<small>{errors.contactPhone}</small>}</label><div className="checks"><label><input type="checkbox" checked={form.showContactPhone} disabled={!form.contactPhone.trim()} onChange={event=>set('showContactPhone',event.target.checked)}/> Afficher mon numéro aux acheteurs sur cette annonce</label></div></fieldset><button className="submit" type="submit">Publier l’annonce →</button>{status&&<p className="formStatus">{status}</p>}</form></main>
}
