import Link from 'next/link'
import {demoListings,formatPrice} from '../../lib/store'
export default function Favorites(){return <main><header className="simpleNav"><Link className="logo" href="/">Dealora<span>.</span></Link><Link href="/compte">Mon compte</Link></header><section className="account"><p className="eyebrow">VOTRE SÉLECTION</p><h1>Mes favoris</h1><div className="grid compactGrid">{demoListings.slice(0,2).map(x=><article className="card" key={x.id}><div className="photo">{x.emoji}<span>♥</span></div><div className="cardbody"><small>{x.mode==='trade'?'Troc possible':'Achat'}</small><h3>{x.title}</h3><strong>{formatPrice(x.price)}</strong><p>🧾 Facture : {x.invoice?'Oui':'Non'} · {x.shipping?'Expédition disponible':'Remise en main propre'}</p></div></article>)}</div></section></main>}
